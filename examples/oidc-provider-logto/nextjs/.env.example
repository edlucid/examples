# Logto Sample Environment Variables

# The Client ID obtained from the Account configured in LTI Bridge (OIDC_IDP method)
LOGTO_APP_ID=

# A client secret (may not be strictly needed for IdP-initiated flow but required by SDK)
LOGTO_APP_SECRET=dummy-secret-for-idp-flow

# The OIDC endpoint of your LTI Bridge instance (Base URL + /oidc, e.g., http://localhost:3000/oidc)
LOGTO_ENDPOINT=http://localhost:3000/oidc

# The base URL where this sample app runs (e.g., http://localhost:3001)
BASE_URL=http://localhost:3001

# A strong secret for cookie encryption (generate with: openssl rand -hex 32)
LOGTO_COOKIE_SECRET=